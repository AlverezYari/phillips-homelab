apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: tailscale-config
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: staging
  target:
    creationPolicy: Owner
  data:
  - secretKey: tailscale_client_id
    remoteRef:
      key: operator-oauth
      property: client_id
  - secretKey: tailscale_client_secret
    remoteRef:
      key: operator-oauth
      property: client_secret
