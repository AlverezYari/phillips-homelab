apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: tycho-pg
  namespace: tycho
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: tycho-pg
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s
