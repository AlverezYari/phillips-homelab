apiVersion: v1
kind: Service
metadata:
  name: atuin
  namespace: atuin
  labels:
    app.kubernetes.io/name: atuin
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: atuin
    app.kubernetes.io/component: server
  ports:
    - name: http
      port: 8888
      targetPort: http
