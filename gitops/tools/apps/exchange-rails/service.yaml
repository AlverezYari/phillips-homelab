apiVersion: v1
kind: Service
metadata:
  name: exchange-rails
  namespace: tatl
  labels:
    app.kubernetes.io/name: exchange-rails
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: exchange-rails
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
