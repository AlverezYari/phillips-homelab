apiVersion: v1
kind: Service
metadata:
  name: buildkit
  namespace: buildkit
  labels:
    app.kubernetes.io/name: buildkit
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: buildkit
  ports:
    - name: grpc
      port: 1234
      targetPort: 1234
      protocol: TCP
